{
  "name": "bsv-prediction-market",
  "version": "0.0.0",
  "private": true,
  "description": "Feasibility spike: native on-chain UTXO LMSR prediction market on BSV via R\u00fanar",
  "type": "module",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "setup": "bash scripts/setup.sh",
    "dev": "bash scripts/dev.sh",
    "demo": "node --import tsx apps/spike/src/seed-demo.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p packages/lmsr/tsconfig.json && tsc --noEmit -p packages/persistence/tsconfig.json && tsc --noEmit -p packages/engine/tsconfig.json && tsc --noEmit -p packages/execution/tsconfig.json && tsc --noEmit -p packages/wallet/tsconfig.json && tsc --noEmit -p packages/contracts/tsconfig.json && tsc --noEmit -p apps/spike/tsconfig.json && tsc --noEmit -p apps/daemon/tsconfig.json && tsc --noEmit -p apps/web/tsconfig.json",
    "test:ui": "vitest run apps/web/test/ui-journey.test.tsx",
    "daemon": "pnpm --filter @pm/daemon dev",
    "web": "pnpm --filter @pm/web dev",
    "db:migrate": "node --import tsx packages/persistence/src/migrate-cli.ts"
  },
  "devDependencies": {
    "jsdom": "^25",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
