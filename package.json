{
  "name": "bsv-prediction-market",
  "version": "0.0.0",
  "private": true,
  "description": "Feasibility spike: native on-chain UTXO LMSR prediction market on BSV via Rúnar",
  "type": "module",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p packages/lmsr/tsconfig.json && tsc --noEmit -p packages/persistence/tsconfig.json && tsc --noEmit -p packages/engine/tsconfig.json && tsc --noEmit -p packages/contracts/tsconfig.json && tsc --noEmit -p apps/spike/tsconfig.json && tsc --noEmit -p apps/daemon/tsconfig.json",
    "test": "vitest run",
    "db:migrate": "node --import tsx packages/persistence/src/migrate-cli.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
